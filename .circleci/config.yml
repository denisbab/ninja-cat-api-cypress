version: 2.1
orbs:
  cypress: cypress-io/cypress@2.2.0
workflows:
  build:
    jobs:
      - cypress/install
      - cypress/run:
          requires:
            - cypress/install
    start: npm start
          parallel: true
          parallelism: 4
    record: true
          group: 4 machines